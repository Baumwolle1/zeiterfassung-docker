services:
  zeiterfassung:
    build: .
    container_name: zeiterfassung
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - zeiterfassung_data:/app/data

volumes:
  zeiterfassung_data:
